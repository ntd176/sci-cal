<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- base application theme. -->
    <style name="Theme.ScientificCalculator" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- customize your theme here. -->
    </style>

    <!-- style cơ bản cho tất cả các nút máy tính -->
    <style name="CalculatorButtonBase" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/tanAccent</item>
    </style>

    <!-- Style cho các nút chức năng (rad, pi, sin, cos, ...) -->
    <style
        name="ScientificFunctionButton" parent="CalculatorButtonBase">
    </style>

    <!-- style cho các nút số (0-9) -->
    <style
        name="NumberButton" parent="CalculatorButtonBase">
    </style>

    <!-- Style cho các nút phép toán cơ bản (+, -, x, /) -->
    <style
        name="OperatorButton" parent="CalculatorButtonBase">
    </style>

    <!-- Style cho các nút đặc biệt như Clear, Equals -->
    <style
        name="SpecialButton" parent="CalculatorButtonBase">
    </style>
</resources>